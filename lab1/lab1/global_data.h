#pragma once
#include"binNumber.h"

const static binNumber posInt(31, 30, -1), posFloat(31, 30, 22), posDouble(63, 62, 51), posLong(31, 30, -1);//position sign,order & mantissa for different types
extern binNumber color;